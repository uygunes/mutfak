<%= semantic_form_for @stok do |f| %>
  <%= f.inputs do %>
    <%= f.collection_select :malzeme_id, Malzeme.all, :id, :name_with_city, {:prompt => "Malzeme sec"}%>
    <%= f.number_field :miktar,placeholder: "Miktar" %>
   
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit, :as => :input %>
  <% end %>
<% end %>
